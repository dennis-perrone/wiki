# Implementing Multiple Plays in Ansible

## Notes

- To view all collections within executable environment:
  - Interactively: `ansible-navigator collections list`.
  - Non-interactive: `ansible-navigator collections list -m stdout`
